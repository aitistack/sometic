# @sometic/http

Fetch-first HTTP client: interceptors, retry/backoff, request dedupe, typed errors, and an optional auth refresh queue that integrates with `@sometic/auth` without coupling auth core to HTTP.
