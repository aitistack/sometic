# `@sometic/cli`

Explicit Sometic installer. **No postinstall prompts.**

```bash
pnpm dlx @sometic/cli@latest init
pnpm dlx @sometic/cli@latest add button
```

Modes: `hybrid` (default), `package`, `source`.

Docs: https://sometic.aitistack.com
