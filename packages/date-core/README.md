# `@sometic/date-core`

Date adapter contract for Sometic. No date library is bundled.

## Install

```bash
pnpm add @sometic/date-core
```

Use with `@sometic/date-native` (default) or optional `@sometic/date-dayjs` / `@sometic/date-fns`.

## License

MIT
