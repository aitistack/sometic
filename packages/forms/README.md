# @sometic/forms

Framework-independent form engine: registration, field arrays, async validation, server errors, drafts, steps, FormData, and a11y helpers.

Depends on `@sometic/validation`. See `docs/consumer/forms/` and Phase 9 docs.
