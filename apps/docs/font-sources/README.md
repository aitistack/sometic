# Font sources (not shipped)

TTF / variable sources used to generate the latin WOFF2 files under `apps/docs/public/fonts/`.

Only WOFF2 (+ OFL) are served by the docs site. Keep this folder out of `public/` so production builds stay lean.
