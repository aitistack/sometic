# Accessibility, Overview

Reusable focus, keyboard, dismissable, portal, scroll-lock, announcer, and observer engines. Prefer subpath imports. Compose from components, do not reimplement conflicting focus logic in adapters.
