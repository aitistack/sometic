# Firebase

`createFirebaseAuthProvider({ auth })`, pass a Firebase Auth instance (peer `firebase`).

Email/password sign-in/register, sign-out, ID token refresh via `getIdToken(true)`, password reset, optional email verification.

Does not bundle the Firebase SDK.
