# Validation overview

`@sometic/validation` provides a shared issue model and native validators without locking you into Zod, Yup, or Valibot.

Use it alone for value checks, or with `@sometic/forms` for form-wide validation.

See consumer docs under `docs/consumer/validation/`.
