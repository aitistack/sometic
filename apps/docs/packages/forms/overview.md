# Forms overview

`createForm` owns values, field meta (dirty/touched/visited/pending), sync/async validation, server errors, submit, field arrays, drafts, and steps, without a UI framework.

Wave A adapters: React hooks, Vue composables, Vanilla controllers, `sometic-form` element.
