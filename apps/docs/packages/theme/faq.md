# Theme, FAQ

SSR-safe (no import-time browser globals). `mode: "system"` follows `prefers-color-scheme`. Persist with `persist: true` + storage adapter. Presets stay on a subpath to protect the root size budget.
