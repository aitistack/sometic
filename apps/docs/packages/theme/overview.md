# Theme, Overview

`@sometic/theme` owns tokens, CSS variable generation, system preferences, contrast helpers, and a theme controller.

Import the controller from `@sometic/theme` and optional surfaces from `/tokens`, `/css-variables`, `/contrast`, `/system`, `/presets`.
