# Theme, Comparison

Prefer `@sometic/theme` for framework-neutral runtime theming. Use styling resolvers alone when you only need classes/styles. Use build-time token pipelines when you need design-tool export, feed results into `defineTokens`.
