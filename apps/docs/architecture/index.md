# Architecture

Canonical architecture lives in the repository under `docs/architecture/`.

Highlights:

- Framework-independent cores
- One-way dependency direction
- World-class quality bar (edges + FAQs)
- Hybrid CLI later; no postinstall prompts
