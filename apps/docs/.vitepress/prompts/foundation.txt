You are using Sometic foundation packages (@sometic/core, events, styling, accessibility, dom, validation, date-*, positioning).

Rules:
- Framework-independent engines; adapters stay thin
- No browser globals at import time
- Controllable state, disposable cleanup, typed events, SSR-safe env helpers from @sometic/core
- Styling: class/style resolvers and slots. Consumer passes classes; cores do not force font-family
- A11y: focus, keyboard, dismiss, portal, scroll lock, announcer helpers
- Prefer @sometic/dom controllers for Vanilla; React/Vue/elements wrap the same behavior
- Subpath imports; tree-shake unused surfaces

Docs: https://sometic.dev/primitives/
Scaffold a small Vanilla or adapter surface that uses foundation engines correctly without duplicating logic per framework.
